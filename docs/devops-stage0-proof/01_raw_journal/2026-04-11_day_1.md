# Build Journal - Day 1: 2026-04-11

## SESSION START: 2026-04-11 08:32:26 +01:00

- current day/date: Day 1 / 2026-04-11
- target gate: Gate A
- checkpoint: target repo activated locally; required artifacts present; no live provider, domain, or checker-key inputs supplied yet
- blockers: provider access missing; domain control missing; exact HNG username unconfirmed; checker public SSH key missing
- next three actions:
  - capture provider, distro, public IP, domain, and exact HNG username once supplied
  - keep a known-good session open while creating `hngdevops` and testing a second login
  - verify the exact sudoers rule and retained access before any firewall change
- logs to update this session:
  - sprint progress tracker
  - delivery progress tracker
  - proof progress tracker
  - sprint, delivery, and proof evidence and decisions logs

## Session focus

- confirm this repo is the target project repo and start Day 1 logging here
- record the provider, distro, domain, exact HNG username, and checker-key status honestly
- lock the minimum safe SSH-hardening sequence before touching the live server

## What I changed

- read `AGENTS.md`, the three sprint prompts, `docs/agent-ops/README.md`, `docs/agent-ops/04_deadline-overlay.md`, `docs/agent-ops/06_delivery-state.md`, and the active sprint, delivery, and proof trackers
- verified that all required sprint, delivery, and proof artifact paths already exist in this repo
- initialized git on `main` and set `origin` to `https://github.com/Kaycee-dev/devops-stage0-nginx-server.git`
- added `.gitignore` rules so `.agents/`, `AGENTS.md`, the sprint prompts, and `docs/` stay local-only
- created commit `f7487a2` with the public repo surface only: `.gitignore` and `README.md`
- updated the sprint, delivery, proof, and delivery-state trackers for Day 1 session start
- expanded the server hardening plan with the retained-access sequence, exact sudoers rule, and Gate A blockers

## Decision points

- treat this working directory as the authoritative target repo, not a copy-only overlay
- keep the control-plane bundle local-only and out of the public repo
- stop before live server changes because provider access, domain control, exact HNG username, and the checker key are still missing

## What broke and how I fixed it

- unrestricted shell reads were required after the local sandbox failed on simple repo reads; approval was requested and the sprint files were then read directly
- `git status` and `git remote -v` initially failed because the directory was not yet a git repo; `git init -b main` and `git remote add origin ...` resolved that repo-state blocker
- the first commit attempt included local-only agent and sprint files; `.gitignore` was updated, those paths were removed from the index, and the public repo surface was committed cleanly

## Verification captured

- `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'` returned `2026-04-11 08:32:26 +01:00`
- `git status --short --branch` initially reported "not a git repository", then reported `## No commits yet on main`
- `git remote -v` now shows `origin` at `https://github.com/Kaycee-dev/devops-stage0-nginx-server.git` for fetch and push
- `git log --oneline -1` now shows `f7487a2 Initialize project repo surface`
- `git status --short --branch --ignored` now shows `## main` with `.agents/`, `AGENTS.md`, the sprint prompts, and `docs/` ignored locally
- required artifact paths under `docs/devops-stage0-sprint/`, `docs/devops-stage0-delivery/`, and `docs/devops-stage0-proof/` are present
- live provider, distro, domain, exact HNG username, and checker-key facts are still unverified

## Next starting point

- decide whether to push commit `f7487a2` to `origin`
- obtain provider console or SSH access, the real domain and DNS control path, the exact HNG username, and the checker public SSH key
- once safe access exists, create `hngdevops`, install the operator key, and test a second-session login before changing `sshd`
